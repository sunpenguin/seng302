junit:
  stage: test
  script:
    - mvn -B clean test

generate_artifacts:
  stage: deploy
  script:
    - mvn -B clean site package
#    - mvn -B clean package
    - tar -czf target/site.tar.gz target/site
  artifacts:
    paths:
      - gameX/*/target/*.jar
      - target/site.tar.gz